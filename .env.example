DISCORD_TOKEN=
DISCORD_CLIENT_ID=

# 32-byte key in base64, prefixed with base64:
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=base64:

DATABASE_URL=postgresql://user:pass@host:5432/gemstone_tools?sslmode=require

# Optional default keys (alliances can set their own via /setup_alliance)
PNW_DEFAULT_API_KEY=
PNW_DEFAULT_BOT_KEY=
